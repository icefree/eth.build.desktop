---
📋 项目概述

将 ETH.Build（基于 React + LiteGraph.js 的 Web3 可视化编程教育工具）转换为 Tauri 桌面应用，并集成本地以太坊测试网络和管理控制面板。
---

🏗️ 实现计划

阶段一：Tauri 基础集成 (预计 2-3 天)

1.1 初始化 Tauri 项目

任务：
├── 安装 Tauri CLI 和依赖 (@tauri-apps/cli, @tauri-apps/api)
├── 运行 `tauri init` 初始化项目结构
├── 配置 tauri.conf.json（窗口设置、应用元数据）
├── 调整 package.json scripts 添加 tauri 相关命令
└── 解决 Node polyfills 兼容性问题（crypto, buffer 等）

1.2 React 适配调整

任务：
├── 移除/替换浏览器特有 API（QR Scanner、某些 Web3 Provider）
├── 添加 Tauri IPC 通信层 (invoke/listen)
├── 配置 CSP 安全策略允许本地 RPC 通信
└── 调整 webpack/vite 配置支持 Tauri

1.3 项目结构

eth.build/
├── src/ # 现有 React 代码
├── src-tauri/ # 新增 Tauri Rust 后端
│ ├── src/
│ │ ├── main.rs
│ │ ├── lib.rs
│ │ ├── commands/ # Tauri Commands
│ │ ├── ethereum/ # 本地测试网管理
│ │ └── services/ # 后台服务管理
│ ├── Cargo.toml
│ └── tauri.conf.json
└── package.json

---

阶段二：本地以太坊测试网络 (预计 3-4 天)

2.1 集成 Anvil/Hardhat Node

任务：
├── 内嵌 Anvil (Foundry) 二进制文件或使用 Hardhat 作为子进程
├── Rust 端管理进程生命周期 (std::process::Command)
├── 支持配置：
│ ├── 初始账户数量和余额
│ ├── Chain ID 设置
│ ├── 区块时间（手动/自动）
│ ├── Gas 价格设置
│ └── Fork 模式（可选连接主网数据）
└── 暴露 Tauri Commands 控制网络启停

2.2 Rust 后端服务模块

// src-tauri/src/ethereum/mod.rs
pub struct LocalNetwork {
process: Option<Child>,
rpc_url: String,
accounts: Vec<Account>,
config: NetworkConfig,
}

impl LocalNetwork {
pub fn start(&mut self) -> Result<(), Error>;
pub fn stop(&mut self) -> Result<(), Error>;
pub fn mine_block(&self) -> Result<Block, Error>;
pub fn set_auto_mine(&mut self, enabled: bool);
pub fn get_accounts(&self) -> Vec<Account>;
}

2.3 Tauri Commands 定义

#[tauri::command]
async fn start_local_network(config: NetworkConfig) -> Result<NetworkInfo, String>;

#[tauri::command]
async fn stop_local_network() -> Result<(), String>;

#[tauri::command]
async fn mine_block() -> Result<BlockInfo, String>;

#[tauri::command]
async fn get_accounts() -> Result<Vec<AccountInfo>, String>;

#[tauri::command]
async fn faucet(address: String, amount: String) -> Result<TxHash, String>;

---

阶段三：领水功能 (Faucet) (预计 1-2 天)

3.1 Faucet 服务实现

功能：
├── 从预置账户向指定地址转账
├── 支持自定义金额（默认 10 ETH）
├── 交易记录保存
├── 余额检查和限制
└── UI 组件集成

3.2 前端 Faucet 组件

// src/components/Faucet/index.jsx

- 输入框：接收地址
- 金额选择：1/5/10/100 ETH
- 领取按钮
- 交易状态显示
- 历史记录列表

---

阶段四：Transaction 详情查看器 (预计 2-3 天)

4.1 Transaction Explorer 组件

功能：
├── 交易列表视图（实时更新）
├── 交易详情展示：
│ ├── Transaction Hash
│ ├── Block Number
│ ├── From / To 地址
│ ├── Value (ETH)
│ ├── Gas Used / Gas Price
│ ├── Input Data (decoded if ABI available)
│ ├── Logs / Events
│ └── 状态（Pending/Success/Failed）
├── 搜索和筛选功能
└── 与现有节点系统集成

4.2 数据结构

interface TransactionDetail {
hash: string;
blockNumber: number;
from: string;
to: string;
value: string;
gasUsed: string;
gasPrice: string;
input: string;
decodedInput?: DecodedData;
logs: Log[];
status: 'pending' | 'success' | 'failed';
timestamp: number;
}

---

阶段五：手动挖矿功能 (预计 1 天)

5.1 挖矿控制

功能：
├── 切换自动/手动挖矿模式
├── 手动挖矿按钮（mine single block）
├── 批量挖矿（mine N blocks）
├── 设置自动挖矿间隔
├── 当前区块高度显示
└── Pending 交易队列显示

5.2 LiteGraph 新节点

// src/nodes/LocalNet/MineBlock.js

- 触发挖矿的节点
- 可连接到其他节点实现自动化流程

// src/nodes/LocalNet/BlockHeight.js

- 实时显示当前区块高度

---

阶段六：控制面板 (Control Panel) (预计 3-4 天)

6.1 控制面板 UI 设计

布局：
┌─────────────────────────────────────────────────────────┐
│ ETH.Build Desktop [─][□][×]│
├──────────┬──────────────────────────────────────────────┤
│ │ │
│ Network │ [原有 LiteGraph Canvas 区域] │
│ Status │ │
│ ────── │ │
│ ● RPC │ │
│ ○ WS │ │
│ │ │
│ Accounts│ │
│ ────── │ │
│ [Acc 1] │ │
│ [Acc 2] │ │
│ ... │ │
│ │ │
│ Actions │ │
│ ────── │ │
│ [Mine] │ │
│ [Faucet]│ │
│ │ │
├──────────┴──────────────────────────────────────────────┤
│ [Console / Logs] [▲][▼]│
└─────────────────────────────────────────────────────────┘

6.2 控制面板功能模块

模块：
├── 网络状态面板
│ ├── RPC 端口状态和 URL
│ ├── WebSocket 端口状态
│ ├── Chain ID 显示
│ ├── 当前区块高度
│ └── 网络启动/停止按钮
│
├── 账户管理面板
│ ├── 预置账户列表
│ ├── 余额显示
│ ├── 私钥复制
│ ├── 导入/导出账户
│ └── 快速 Faucet 按钮
│
├── 服务管理面板
│ ├── 本地节点进程状态
│ ├── 端口占用检测
│ ├── 日志查看
│ ├── 重启服务按钮
│ └── 端口配置（RPC: 8545, WS: 8546）
│
└── 交易/区块浏览器面板
├── 最近交易列表
├── 最近区块列表
├── Pending 交易队列
└── 详情查看入口

6.3 端口和服务管理

// src-tauri/src/services/port_manager.rs
pub struct PortManager {
allocated_ports: HashMap<String, u16>,
}

impl PortManager {
pub fn allocate_port(&mut self, service: &str, preferred: u16) -> Result<u16, Error>;
pub fn release_port(&mut self, service: &str);
pub fn check_port_available(port: u16) -> bool;
pub fn get_service_port(&self, service: &str) -> Option<u16>;
}

// 服务列表

- ethereum_rpc: 8545 (可配置)
- ethereum_ws: 8546 (可配置)

---

阶段七：集成与优化 (预计 2-3 天)

7.1 与现有节点系统集成

任务：
├── 修改 src/nodes/Web3/ 节点默认连接本地网络
├── 添加网络选择器（Local/Mainnet/Testnet）
├── MetaMask 节点适配（可选禁用或模拟）
├── 新增 LocalNet 节点类别：
│ ├── LocalNet/Connect
│ ├── LocalNet/Faucet
│ ├── LocalNet/Mine
│ ├── LocalNet/Accounts
│ └── LocalNet/TxExplorer
└── 自动检测本地网络状态

7.2 数据持久化

存储：
├── 应用配置 (tauri store plugin)
├── 网络配置和状态
├── 账户数据（加密存储）
├── 交易历史
└── 用户偏好设置

7.3 性能优化

优化项：
├── 减少内存占用（移除不必要的 Web3 providers）
├── 懒加载大型模块
├── 优化 IPC 通信频率
└── 区块/交易数据分页加载

---

📁 新增文件结构

eth.build/
├── src-tauri/
│ ├── src/
│ │ ├── main.rs # Tauri 入口
│ │ ├── lib.rs # 库导出
│ │ ├── commands/
│ │ │ ├── mod.rs
│ │ │ ├── network.rs # 网络控制命令
│ │ │ ├── accounts.rs # 账户相关命令
│ │ │ ├── mining.rs # 挖矿命令
│ │ │ └── transactions.rs # 交易查询命令
│ │ ├── ethereum/
│ │ │ ├── mod.rs
│ │ │ ├── local_network.rs # 本地网络管理
│ │ │ ├── faucet.rs # 领水服务
│ │ │ └── types.rs # 类型定义
│ │ └── services/
│ │ ├── mod.rs
│ │ ├── port_manager.rs # 端口管理
│ │ └── process_manager.rs # 进程管理
│ ├── Cargo.toml
│ ├── tauri.conf.json
│ └── binaries/ # 内嵌二进制（anvil）
│
├── src/
│ ├── components/ # 新增 UI 组件
│ │ ├── ControlPanel/
│ │ │ ├── index.jsx
│ │ │ ├── NetworkStatus.jsx
│ │ │ ├── AccountsPanel.jsx
│ │ │ ├── MiningControl.jsx
│ │ │ └── ServiceManager.jsx
│ │ ├── Faucet/
│ │ │ └── index.jsx
│ │ ├── TxExplorer/
│ │ │ ├── index.jsx
│ │ │ ├── TxList.jsx
│ │ │ └── TxDetail.jsx
│ │ └── BlockExplorer/
│ │ └── index.jsx
│ ├── hooks/
│ │ ├── useTauri.js # Tauri IPC hooks
│ │ ├── useLocalNetwork.js
│ │ └── useAccounts.js
│ ├── nodes/
│ │ └── LocalNet/ # 新增节点类别
│ │ ├── index.js
│ │ ├── Connect.js
│ │ ├── Faucet.js
│ │ ├── Mine.js
│ │ ├── Accounts.js
│ │ └── TxWatch.js
│ └── ...
└── package.json

---

📦 技术栈补充
┌───────────┬───────────────────────────────────┐
│ 组件 │ 技术选型 │
├───────────┼───────────────────────────────────┤
│ 桌面框架 │ Tauri v2 │
├───────────┼───────────────────────────────────┤
│ Rust 后端 │ tokio, serde, ethers-rs │
├───────────┼───────────────────────────────────┤
│ 本地节点 │ Anvil (Foundry) / Hardhat │
├───────────┼───────────────────────────────────┤
│ UI 组件 │ Material-UI (已有) + 新增面板组件 │
├───────────┼───────────────────────────────────┤
│ 状态管理 │ React Context + Tauri Events │
├───────────┼───────────────────────────────────┤
│ 持久化 │ tauri-plugin-store │
└───────────┴───────────────────────────────────┘

---

⏱️ 时间估算
┌────────────────────────────┬──────────┐
│ 阶段 │ 预计时间 │
├────────────────────────────┼──────────┤
│ 阶段一：Tauri 基础集成 │ 2-3 天 │
├────────────────────────────┼──────────┤
│ 阶段二：本地测试网络 │ 3-4 天 │
├────────────────────────────┼──────────┤
│ 阶段三：领水功能 │ 1-2 天 │
├────────────────────────────┼──────────┤
│ 阶段四：Transaction 查看器 │ 2-3 天 │
├────────────────────────────┼──────────┤
│ 阶段五：手动挖矿 │ 1 天 │
├────────────────────────────┼──────────┤
│ 阶段六：控制面板 │ 3-4 天 │
├────────────────────────────┼──────────┤
│ 阶段七：集成与优化 │ 2-3 天 │
├────────────────────────────┼──────────┤
│ 总计 │ 14-20 天 │
└────────────────────────────┴──────────┘
